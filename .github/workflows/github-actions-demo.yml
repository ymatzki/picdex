name: Deploy
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: yarn build
      - run: yarn deploy
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}